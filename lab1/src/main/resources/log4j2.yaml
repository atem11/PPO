Configuration:
  appenders:
    Console:
      name: stdout
      PatternLayout:
        Pattern: "%d %-5level  [%t] %c: %X{formatted-rid}%msg%n%throwable"
  Loggers:
    Root:
      level: info
      AppenderRef:
        - ref: stdout

